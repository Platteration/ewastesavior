# SaviorOS module settings.

# p4-clockmod "throttles" old Pentium 4s by stopping the clock, which only
# slows them down; SaviorOS does its own thermal policy.
blacklist p4-clockmod

# USB sticks: start scanning after 1 s instead of waiting longer.
options usb-storage delay_use=1
